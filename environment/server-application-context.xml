<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
   xmlns:p="http://www.springframework.org/schema/p" xmlns:context="http://www.springframework.org/schema/context"
   xmlns:camel="http://camel.apache.org/schema/spring"
   xsi:schemaLocation="
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.2.xsd
        http://camel.apache.org/schema/spring http://camel.apache.org/schema/spring/camel-spring.xsd"
   default-init-method="init" default-destroy-method="destroy">

   <import resource="database-application-context.xml" />

   <!-- JdbcTemplate used in our DAOs -->
   <bean id="NamedParameterJdbcTemplate" class="org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate">
      <constructor-arg>
         <ref bean="DatabaseDriver" />
      </constructor-arg>
   </bean>

   <!-- DAO to insert and retrieve information from database -->
   <bean id="UserDao" class="com.jjw.sandbox.dao.impl.UserJdbcDaoImpl" />

   <!-- Server responsible for handling incoming JMS messages -->
   <bean id="Server" class="com.jjw.sandbox.server.Server" />

   <!-- Receives message over ActiveMQ -->
   <camelContext id="SandboxServer" xmlns="http://camel.apache.org/schema/spring">

      <!-- Loosely couple the Java code from knowing any of the JMS queues/topics. -->
      <endpoint id="JmsServer" uri="${sandbox.jms.endpoint}" />

      <route>
         <from uri="ref:JmsServer" />
         <log message="Received a message with body: ${body} and headers: ${headers}" />
         <bean ref="Server" />
      </route>

      <route>
         <from uri="jms:queue:primary" />
         <log message="jms:queue:primary" />
         <to uri="mock:primary" />
      </route>
   </camelContext>

</beans>